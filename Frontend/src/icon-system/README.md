
# Dink1 Icon System (ZIP 2)

Includes:
- Icon component
- useIconPack hook
- Dynamic pack loader

Usage:
<Icon name="home" size={28} />

Switch pack:
const { setIconPack } = useIconPack()
setIconPack('solid')
